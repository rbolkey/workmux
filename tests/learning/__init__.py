# Learning tests verify assumptions about external dependencies.
# Each subdirectory targets a specific dependency (e.g., cmux/).
# These tests are NOT part of the regular test suite -- they require
# the dependency's runtime environment to be active.
